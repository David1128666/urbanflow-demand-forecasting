urbanflow {
  kafka {
    bootstrap-servers = "localhost:9092"
    bootstrap-servers = ${?KAFKA_BOOTSTRAP_SERVERS}
    demand-topic = "urbanflow-demand-events"
    demand-topic = ${?KAFKA_DEMAND_TOPIC}
  }
}
